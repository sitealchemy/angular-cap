import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'itbeess.angcap.com',
  appName: 'angular-cap',
  webDir: 'dist/angular-cap',
  server: {
    androidScheme: 'https'
  }
};

export default config;
